# known-socially

App for aggregating social medai accounts

Commands

```bash
# Start the database
docker compose up

# Start the Go service
go build . && PORT=80 ./${PWD##*/}

# Close the database
docker compose up
```
